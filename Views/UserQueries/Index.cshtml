@model IEnumerable<WebApplication1.Models.UserQuery>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.queryDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.queryDescripion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Users.userName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.queryDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.queryDescripion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.userName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.queryNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.queryNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.queryNo })
        </td>
    </tr>
}

</table>
