@model IEnumerable<WebApplication1.Models.ShoppingCartItemBridge>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Product.productName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCarts.cartID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.productName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShoppingCarts.cartID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.bridgeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.bridgeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.bridgeID })
        </td>
    </tr>
}

</table>
